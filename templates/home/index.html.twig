{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">

        <h2 class="text-center my-5">Annonces de vente d'objet</h2>
        <div class="row">
{#            {% for annonce in annonces %}#}
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm">
                        <img class="card-img-top" src="" alt="">
                        <div class="card-body">
                            <h5 class="card-title"></h5>
                            <p class="card-text"></p>
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted"> €</small>
                                <a href="" class="btn btn-sm btn-outline-primary">Voir l'annonce</a>
                            </div>
                        </div>
                    </div>
                </div>
{#            {% endfor %}#}
        </div>
    </div>
{% endblock %}
